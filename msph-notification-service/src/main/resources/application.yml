spring:
  application:
    name: msph-notification-service
  cloud:
    vault:
      token: ${MSPH_VAULT_ROOT_TOKEN}
      authentication: TOKEN
      kv:
        backend: moonsphere
        default-context: msph-common
        application-name: ${spring.application.name}
